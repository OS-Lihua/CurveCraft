＃＃ 背景
我要写一个脚本，它能从外部（API接口/其他组件）输入一个函数表达式（任意一元函数 F(x,y)=0））和一个二元组 （xl，xr）。这个脚本要干3个事情：1.判断该函数在第一象限内是否是单调函数（横轴为x，纵轴为y的情况下）如果不是，向前端放回 error。2. 用多项式插值的方法将该函数在 (xl, xr) 上拟合成 n次多项式 y=f(x)，得到系数数列 {a_n}。同理拟合出 x=g(y) 的系数数列 {b_n}，n 是脚本内的可调参数。3. 判断该 y=f(x) 是否是 (xl, xr) 上的单调减函数，如果不是向 =g(y) 的系数数列 {b_n}，n 是脚本内的可调参数。3. 判断该 y=f(x) 是否是 (xl, xr) 上的单调减函数，如果不是向前端返回 error。如果是，则向后端返回 3个东西：{数列长度:n+1,  y=f(x)的系数数列 {a_n} ，x=g(y) 的系数数列 返回 error。如果是，则向后端返回 3个东西：{数列长度:n+1,  y=f(x)的系数数列 {a_n} ，x=g(y) 的系数数列 {b_n} }。” 这个任务能把它做成一个txs组件吗？这样 craft 的 page里面的参数就可以传到这个组件里面，这个组件计算出多项式数组后也可以传到其他地方。

## 任务
撤回之前ｔｓ以python混合编程的方法，删除相关代码，将这个任务改成搭一个python服务器，服务器的API接口遵循，输入一个函数表达式（任意一元函数 F(x,y)=0））和一个二元组 （xl，xr）；输出{数列长度:n+1,  y=f(x)的系数数列 {a_n} ，x=g(y) 的系数数列 {b_n} }。具体任务和上面的这个task相同。craftcurve改成和这个服务器连接，